#include <elf.h>
#include <stdio.h>
#include <stdlib.h>
/**
 * append_text_to_file - appends text at the end of a file.
 * @elfFile: elfFile name
 * Return: 1 on success, -1.
 */
void read_elf_header(const char* elfFile) {

}
/**
 * main - check the code
 * @ac: arguments count
 * @av: arguments vactor
 * Return: 0 success Otherwise failed
 */
int main(int ac, char **av)
{
	
	exit(98);
}
