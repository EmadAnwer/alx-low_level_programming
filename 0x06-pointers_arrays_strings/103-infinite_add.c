/*
 * 0x06. C - More pointers, arrays and strings
 * task 6
 */
#include "main.h"
/**
 * infinite_add - capitalizes all words of a string
 * @n1: string refrance
 * @n2: string refrance
 * @r: string refrance
 * @size_r: string refrance
 * Return: *src "all words of a string capital"
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	n1[0] = 5;
	return (n2);
}
